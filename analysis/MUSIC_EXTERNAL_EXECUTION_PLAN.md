# External execution and numerical replay plan v1

2026-09-17; written before the full external run. This operational supplement does not change MUSIC_EXTERNAL_PROTOCOL.md, its participants, sampling, endpoints, or official method arguments.

Before fitting the grid, compare pooling all four eligible held-out donors (240 targets) with four separate official 60-target calls for the same three references used by the frozen technical gate: HC-502 / local triple 0 / block 0, B60 balanced, B120 ratio4 with IGTB256 dominant, B300 ratio10 with IGTB256 dominant. Require maximum prediction difference <=1e-10, exact convergence/effective-support/finite-variance flags and target alignment, and coefficient-sum differences <=1e-10. Recheck the original 18 technical predictions against the matching separate-call rows. Use pooled calls only if all batching checks pass; otherwise preserve the discrepancy and use separate calls. Prediction error direction cannot select the implementation.

Execute all 14 unique donor triples, 90 references per triple, four held-out donors per reference, 60 targets and two outputs: 604800 scientific rows. Maximum three R subprocess workers; the fitting deadline is 10800 seconds from the start of controls, including controls. Preserve partial outputs and failures; no parameter changes, omitted references, or outcome-based retries. Export diagnostics for every weighted fit. The controls remain separately identified and do not replace grid rows.

Freeze a numerical replay case for each of the 14 held-out donors before reading full-grid outcomes. For ring position i starting at zero, choose local triple i modulo 4, block floor(i/3) modulo 3, budget [60,120,300][i modulo 3], and allocation [balanced,ratio2,ratio4,ratio10][i modulo 4]. For unequal allocations choose the alphabetically first of that reference triple's donors; balanced has no dominant donor. These rules cover all 12 budget/allocation pairs among the first 12 donors. Replay all 60 targets and both outputs for each selected case: 1680 prediction rows. Reconstruct reference membership independently from saved donor/type/block prefixes and selected-cell mapping, then use a separate untraced official caller. Compare all predicted proportions and MAE/RMSE with tolerance 1e-10. This separate implementation provides computational verification, not independent biological validation.

Verify full-grid uniqueness and coverage, metadata, proportions, truths, per-row error recalculation, paired baseline keys, and all diagnostic records before interpretation. Export every donor's absolute errors, imbalance penalties, method penalty differences, budget contrasts, reversal counts, type-specific errors and conditional three-block sampling SD. Primary descriptive quantity remains weighted-MuSiC I(300)-I(60) at 10:1:1, in percentage points, with all 14 paired donor values. Give equal weight to targets, blocks, four triples, three dominant choices at unequal levels, and then donors. A balanced baseline is shared across its contrasts, not counted as three independent observations. Shared sources/folds exclude population P values or ordinary paired-test claims. Retain null and contrary results.
